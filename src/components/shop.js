import React from "react";
import Products from "./products";

// This defines the "products" page in the nav bar in the header.
export default function Shop() {
    return(
        <div className="page">
            <Products />
        </div>
    );
}